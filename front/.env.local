# Development (slug is appended in code, so this hits one local backend on 8001)
NEXT_PUBLIC_SERVER_URL=ws://localhost:8001

# Production (Caddy terminates TLS; slug is appended as a path -> wss://.../test, /obby, ...)
# NEXT_PUBLIC_SERVER_URL=wss://back.notblox.online